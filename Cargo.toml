[package]
name = "astrology_website"
version = "1.0.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
anyhow = "1"
async-trait = "0.1.89"
cfg-if = "1"
chrono = { version = "0.4", features = ["serde"] }
console_error_panic_hook = "0.1"
leptos = "0.8.19"
leptos_meta = "0.8.6"
leptos_router = "0.8.13"
nutype = "0.7.0"
serde = { version = "1.0", features = ["derive"] }
thiserror = "2.0.18"
uuid = { version = "1.23.2", features = ["v4", "v7", "js"] }
wasm-bindgen = "0.2"

axum = { version = "0.8.9", optional = true }
config = { version = "0.15", optional = true }
email_address = { version = "0.2.9", optional = true }
leptos_axum = { version = "0.8.9", optional = true }
lettre = { version = "0.11", default-features = false, features = ["builder", "smtp-transport", "tokio1", "tokio1-rustls", "ring", "rustls-native-certs"], optional = true }
phonenumber = { version = "0.3.9", optional = true }
sqlx = { version = "0.9.0", features = ["runtime-tokio", "tls-rustls", "postgres", "macros", "chrono", "uuid"], optional = true }
teloxide = { version = "0.17.0", optional = true }
tokio = { version = "1", features = ["full"], optional = true }
tracing = { version = "0.1", optional = true }
tracing-subscriber = { version = "0.3", optional = true }

[features]
hydrate = [
    "leptos/hydrate",
]
ssr = [
    "axum",
    "config",
    "email_address",
    "leptos/ssr",
    "leptos_axum",
    "leptos_meta/ssr",
    "leptos_router/ssr",
    "lettre",
    "phonenumber",
    "sqlx",
    "teloxide",
    "tokio",
    "tracing",
    "tracing-subscriber",
]

[package.metadata.leptos]
bin-features = ["ssr"]
lib-features = ["hydrate"]
style-file = "style/main.css"
assets-dir = "public"
site-addr = "127.0.0.1:3000"
browserquery = "defaults"
tailwind-input-file = "style/main.css"
